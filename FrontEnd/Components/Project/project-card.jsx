import React from "react";

export default function ProjectCard({ title, organizationName, memberIcons,borderColor }) {
    return <div className="project-card">
        <div className="title" >{title}</div>
    </div>
}